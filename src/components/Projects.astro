
---
import { projects } from '../data/projects.ts';
import ProjectItem from '../components/ProjectItem.astro';
---

<div class="w-full h-full bg-secondary rounded-xl overflow-y-auto pb-10">
  <h2 class="text-2xl font-bold my-6 text-center">Proyectos</h2>
  <!-- Contenedor grid de una sola columna -->
  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-1 gap-10 justify-items-center px-5">
    {projects.map(project => (
      <ProjectItem 
        nameProject={project.nameProject}
        imageDemo={project.imageDemo}
        urlDemo={project.urlDemo}
        urlRepo={project.urlRepo}
        description={project.description}
        languages={project.languages}
      />
    ))}
  </div>
</div>

